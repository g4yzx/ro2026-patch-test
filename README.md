# 葉子仙境 / YZXRO · V1.0 正式版

> 项目：**葉子仙境**（英文品牌 **YZXRO**）｜署名：**yzxhome · 葉子仙境**
> 服务器：`RO.yzxhome.cn`（登录端口 `6900`）
> 本仓库是**客户端补丁通道**（启动器自动更新的入口），**不存放全量客户端**。

---

## 一、玩家怎么玩

1. **下载 V1.0 全量客户端**（约 4.3 GB）
   - 网盘地址：**【待补】** —— 全量包体积过大，不放 GitHub；上传后在此处填入链接。
   - 完整性校验：对照发行包内的 `MANIFEST.sha256`（逐文件 SHA-256）。
2. **解压到一个全新的空目录**（不要覆盖旧客户端残留）。
3. **双击目录里的 `RoLauncher.exe` 启动**
   - 启动器是**唯一启动入口**，会自动检查并下载最新补丁；更新完成后点「开始游戏」。
   - 请不要直接运行游戏主程序。
4. 进游戏前确认能访问 **`RO.yzxhome.cn`**（登录端口 `6900`），无需手动改任何配置文件。

> 已经在玩的玩家：直接双击启动器即可，它会自动把客户端更新到最新版。

---

## 二、补丁通道（启动器自动更新）

本仓库对外只提供两样东西：

| 内容 | 位置 | 说明 |
| --- | --- | --- |
| 清单 | `version.json` | 启动器读取的唯一入口 |
| 补丁包 | **Release 资产** `patch-1.0.1.zip` | 体积超过仓库单文件 100 MB 上限，只能走 Release（上限 2 GB） |

清单地址（raw）：

```
https://raw.githubusercontent.com/g4yzx/ro2026-patch-test/main/version.json
```

启动器的处理流程：

```
检查更新 → 下载补丁包 → SHA-256 校验 → 解压落位到客户端根 → 全部成功才写状态
（任何一步失败都不动原文件，可安全重试）
```

### 国内可达性：两套机制，缺一不可

| 机制 | 覆盖对象 | 位置 | 能否热更 |
| --- | --- | --- | --- |
| 清单**引导镜像** | `version.json` 本身 | 启动器内置的候选前缀 | ❌（需重发启动器） |
| 清单内 `mirrors` | 补丁包 zip | `version.json` | ✅（改清单即生效） |

启动器遇到 `raw.githubusercontent.com` 的清单地址会**自动展开**加速前缀，顺序为：

```
gh-proxy.com  →  ghfast.top  →  ghproxy.net  →  裸 raw（直连兜底）
```

镜像采用「**前缀式**」拼接（`mirror + origin + url`），因此只能接入前缀型镜像。

---

## 三、V1.0 更新内容

葉子仙境 / YZXRO 正式开服版本。相对上一版整合客户端，V1.0 主要包含：

- **品牌统一**：客户端、服务端与启动器统一为「葉子仙境 / YZXRO」。
- **界面文本中文化**：地图 NPC 显示名、任务列表、成就列表、每日提示等改为中文。
- **任务与提示窗修复**：修复部分任务页文字缺引号、显示不完整的问题。
- **商城价格错位修复**：修正部分商品价格与说明对不上的问题。
- **未知物品修复**：修复登录或打开背包时报「未知物品」的问题。
- **云同步修复**：修复账号与角色设置读取失败、快捷键不同步的问题。
- **启动器**：作为唯一启动入口；支持启动器自身更新与高分辨率屏幕自适应。

---

## 四、仓库布局

```
version.json              启动器读取的清单（唯一入口）
README.md                 本文件
（Release 资产）          patch-1.0.1.zip —— 补丁包，不在仓库内
```

---

## 五、清单契约（`version.json`）

| 字段 | 含义 |
| --- | --- |
| `base` | 基线客户端版本（`20260219`） |
| `latest` | 最新补丁版本号；`patches` 末项的 `ver` 必须与之相等 |
| `minLauncher` | 能消费本清单的启动器最低版本 |
| `origin` | 清单与补丁包的**基址**；`patches[].url` 相对它给出 |
| `mirrors` | 顺序回退的**前缀**，拼在 `origin` 之前；末项必须为空串（= 直连） |
| `patches[]` | `ver` / `url` / `sha256` / `size`，按版本升序 |
| `notes` | 本版更新说明，启动器界面直接展示 |
| `launcher` | 启动器自身更新的信息（版本 / 资产名 / sha256 / 体积） |

补丁包的**最终地址**按如下规则解析：

```
for m in mirrors（按顺序尝试）:
    candidate = m + origin + url
    下载成功 且 sha256 匹配  →  采用该源，停止
```

---

## 六、体积上限

- **GitHub 仓库单文件硬上限 100 MB** ⇒ 补丁包（约 122 MB）与全量客户端（约 4.3 GB）
  都**不能**提交进仓库。补丁包走 **Release 资产**（单文件上限 2 GB）；
  全量客户端走**网盘**。
- 仓库必须 **public** —— 启动器面向玩家运行、不内置任何凭据，
  private 仓库的 raw 地址会要求 token 从而拉取失败。

---

## 七、如何反馈

游戏内问题与崩溃请在玩家群里反馈；也可以在本仓库提 Issue（请勿附带任何账号密码）。

> ⚠️ `version.json` 是**发布脚本生成物**，手工改动会在下次发布时被覆盖。

---

`yzxhome · 葉子仙境` · YZXRO V1.0
