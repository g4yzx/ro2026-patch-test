# 葉子仙境 / YZXRO

> 一个基于 **rAthena** 的仙境传说（Ragnarok Online）模拟器整合项目
> 署名：**yzxhome · 葉子仙境**

---

## 当前版本

| 项目 | 版本 | 说明 |
| --- | --- | --- |
| 客户端内容 | **1.0.7**（2026-09-25） | 本仓库清单 `version.json` 的 `latest` |
| 完整客户端整合包 | **V1.0.7**（2026-09-25 重建） | 3.95 GB（4,243,208,341 B）｜解压后约 5.8 GB｜112,762 个文件 |
| 启动器 | **1.0.5** | `RoLauncher.exe`（随补丁包分发，无需单独下载） |
| 客户端基线 | 2026-02-19 | `base: 20260219` |

> 本版整合包**已包含 1.0.5 / 1.0.6 / 1.0.7 的全部内容**，新玩家下载解压后即是最新版，不需要再打任何补丁。
> 旧版整合包（2026-09-23 的 V1.0 版）已下架，请勿再使用。

---

## 这是什么

本仓库是《葉子仙境 / YZXRO》的**游戏补丁更新通道**，只做一件事：

> 让游戏启动器能自动检查并下载最新补丁。

- 仓库里只放**补丁清单**与**补丁包**；
- **完整的游戏客户端不在这里**（体积太大），请从下方网盘链接下载。

---

## 怎么玩

### 第一次玩（三步）

**第 1 步：下载客户端**（两种任选其一，都能正常玩）

- **V1.0.7 完整客户端**（2026-09-25 重建，已整合全部汉化与修复，**推荐**）
  [**点此下载**](https://pan.baidu.com/s/1cWWPxB2JdGo_YZ9JgUhCBg?pwd=6cx4)　提取码：**`6cx4`**
  - 体积 **3.95 GB**（7z 压缩包 4,243,208,341 B）｜解压后约 **5.8 GB**｜共 **112,762** 个文件
  - 已含 **1.0.5**（任务 / 冒险向导 / 装备属性面板中文化）、**1.0.6**（服装色板 0-553 补全、NPC 身形资源补全）、**1.0.7**（设置界面纯净度修复）
  - **下载解压后即是最新版，无需再打补丁**
- **纯净基础客户端**（体积小，需要自己再打一次补丁）
  [**点此下载**](https://noxararo.com/downloads/client/2026-09-19_full_client.rar)

**第 2 步：解压**

- 解压到一个**全新的空目录**。不要覆盖旧客户端残留；路径尽量**短、用英文**（例如 `D:\YZXRO\`）。
- 如果你下载的是**纯净基础客户端**，还需要**打一次补丁**（下载 V1.0.7 完整客户端的可跳过）：
  打开本仓库右侧的 **Releases** 页面，下载**最新的那一个**补丁包（文件名形如 `patch-1.0.7.zip`，约 226 MB），
  把补丁包**全部解压覆盖到客户端根目录**，遇到同名文件选「全部覆盖」。
  补丁包里已经包含启动器、配置文件与全部更新内容，覆盖完成即是最新版。
- ⚠️ **只需要打「最新的那一个」，不需要按版本逐个打**：补丁包是**全量包**（相对纯净基线的完整内容），
  所以不论你手上是哪个版本、缺过哪几次更新，打最新那一个都能**一步对齐**到当前版本。
- 如果同一版本的 Release 里还看到 `patch-x.y.z.zip.001`、`.002` … 这类**分片文件**，
  那是给**启动器自动更新**用的（分片便于走国内源下载），**手动安装只看整包那一个**即可。

补丁要覆盖到**客户端根目录这一层**，不要多套一层文件夹：

```
D:\YZXRO\                     ← 第 2 步解压出来的客户端根目录
├─ data.grf
├─ Ragexe-2026-02-19_patched.exe
├─ data\  System\  ...
└─ RoLauncher.exe             ← 打补丁后会出现在这一层
```

**第 3 步：双击 `RoLauncher.exe` 开始游戏**

- 启动器是**唯一启动入口**：它会自动检查并更新到最新版本，更新完成后点「开始游戏」。
- 请不要直接运行游戏主程序。
- 启动器已内置服务器地址，**不需要手动修改任何配置文件**。

### 已经在玩的

直接双击 `RoLauncher.exe` 即可 —— 启动器会自动把客户端更新到最新版，**不需要再手动下载补丁包**。

---

## 怎么注册账号

**不需要去网站注册**，在游戏登录界面就能自助建号。

1. 打开 `RoLauncher.exe`，等它更新完，进入登录界面。
2. **账号**栏填「**你想要的账号名 + `_M` 或 `_F`**」：
   - 想要**男**角色 → 账号名后面加 `_M`（例：`zhangsan_M`）
   - 想要**女**角色 → 账号名后面加 `_F`（例：`zhangsan_F`）
3. **密码**栏填你自己设的密码。
4. 点「登录」——第一次这样登录会**自动创建账号**。

登录成功后会进入角色选择界面，在**那里创建角色**就可以开始玩了。

### 账号名和密码的要求（很重要，请先看）

- **账号名（去掉 `_M` / `_F` 之后）至少 6 位**，只能用**英文字母 + 数字**，不要用中文、空格或符号。
  - ✅ 可以：`zhangsan_M`、`abc123_F`
  - ❌ 不行：`abc_M`（去掉后缀只剩 3 位，太短）、`张三_M`（中文）
- **密码至少 6 位**，建议英文 + 数字混合。

> ⚠️ 名字或密码太短时，登录界面只会提示「**账号或密码错误 / 账号不存在**」，
> 看起来像是密码打错了，其实是长度不够 —— 请先按上面的要求检查一遍。

### 注册一次之后，以后怎么登录

- `_M` / `_F` **只在第一次注册时加**。
- 以后每次登录，**只填账号名，不要再加 `_M` / `_F`**。
  - 例：注册时填的是 `zhangsan_M`，以后登录就填 `zhangsan` + 你的密码。

### 注意

- **性别在注册时就定下来了，之后不能修改**，请先想好再注册。
- 一个账号可以创建多个角色，不需要重复注册。
- 账号名注册成功后**不能更改**。
- **忘记密码无法自助找回**，请联系管理员。

---

## 常见问题

### 启动器提示「无写入权限」怎么办？

这是**本机安全软件（杀毒 / 安全卫士 / 企业管控）拦截了启动器的更新文件**导致的，**不是客户端问题**。按顺序试：

1. **装到短路径**：把客户端放到 `D:\YZXRO\` 这类**短、英文、有写权限**的目录；不要放在 `C:\Program Files`、桌面、或含中文/空格的深目录。
2. **加白名单**：把 **`RoLauncher.exe` 与整个客户端目录**加入杀毒软件 / 安全卫士的**信任（白名单）**。
3. **重启后再试**：重启电脑后再点一次启动器 —— 部分拦截在重启前不会释放。
4. **仍不行**：直接下载最新**全量客户端**覆盖安装。

> 更新失败时启动器会**自动回滚，并且不写更新状态**，客户端不会被改坏，**可以安全重试**。

### 提示缺少 `msvcr*.dll` / `mfc*.dll`？

安装「微软常用运行库合集（VC++ 2005-2022）」即可。

### 启动后黑屏 / 闪退？

先运行客户端目录里的 `Setup.exe`，改成**窗口化模式**再试。

### 登录不上 / 一直卡在「正在检查更新」？

- 确认本机能正常上网；
- 检查本机防火墙与代理设置（部分代理会拦截游戏流量）；
- 启动器会自动切换加速线路重试，稍等片刻或重开启动器。

---

## 更新说明

### V1.0.7（2026-09-25）· 客户端资源纯净化

- **修正**：游戏内「设置」界面被写入的调试项 —— 此前的 1.0.5 / 1.0.6 更新包误带了开发阶段的界面缩放实验代码；
  更新后设置界面恢复为标准外观，点击任何选项都不会触发无效指令
- 本次只更新客户端资源文件，**不改变任何数值、掉落与规则**

### V1.0.6（2026-09-25）· 客户端资源补全

- **PC 服装色板补全**：色号 0-553 全部可用（90,851 个文件 / 88.7 MB），修复部分颜色导致的报错
- **NPC 身形资源补全**：修复部分 NPC 出现的「Cannot find File」弹窗
- 全量包重构：任意历史版本都能一步对齐到最新

### V1.0.5（2026-09-25）· 汉化补完

- 「冒险向导」各任务页的任务名、摘要与正文全面汉化（累计 19 批）
- 任务详情页标题与描述汉化；任务需求物品英文名改为官方中文名
- 装备属性面板内的英文条目改为中文
- 启动器启动前自动把域名解析成当前 IP 写入配置，公网 IP 变动后仍可登录

### V1.0.3

- **补丁分发改为「全量包」**：任意历史版本都能一次对齐到最新，不必逐版本按序打补丁
- **修正**：客户端内嵌的服务器地址此前写死为 `127.0.0.1`，导致进游戏提示「服务器连接失败」
- **启动器**：更新源改为 **Gitee 优先**（国内直连，不再依赖加速前缀）；支持补丁**分片下载**

### V1.0（正式版）

- **全面中文化**：NPC 显示名、任务、成就、每日提示
- **修复**：任务页文字缺失、商城价格错位、未知物品报错
- **修复**：账号设置与快捷键云同步失败
- **启动器**：作为唯一启动入口，支持自动更新与高分辨率适配

---

## 使用声明

- 本项目**仅供学习与技术研究**使用，**不允许任何形式的商业用途**。
- 禁止用于搭建**收费服务器**、出售或转售、以及任何形式的盈利行为。
- 禁止将本项目或其衍生物用于商业发行、付费下载或捆绑推广。
- 相关游戏素材与商标版权归原版权方所有，本项目与其无隶属关系。
- 因使用本项目产生的任何后果，由使用者自行承担。

---

## 反馈

游戏内问题与崩溃请在玩家群里反馈；也可以在本仓库提 Issue（**请勿附带账号密码**）。

---

`yzxhome · 葉子仙境` · YZXRO V1.0.7
